# ArchMindCanvas v7.3.3 — Smart Tag Bindings

```powershell
git add .
git commit -m "ArchMindCanvas v7.3.3 - smart tag bindings"
git push origin main
```

Tags support Direct, Variable, Resource, Data, Local and Module Output.
