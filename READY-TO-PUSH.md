# ArchMindCanvas v7.9.10 — Clean Canvas
Removed the unwanted information strip above the canvas (objects, connections, score and hierarchy path).

```powershell
git add .
git commit -m "ArchMindCanvas v7.9.10 - remove canvas status bar"
git push origin main
```
