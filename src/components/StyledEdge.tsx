import { BaseEdge, EdgeLabelRenderer, getBezierPath, getSmoothStepPath, getStraightPath, type EdgeProps } from '@xyflow/react';
import type { CanvasEdge } from '../types';
export default function StyledEdge(props:EdgeProps<CanvasEdge>){const{id,sourceX,sourceY,targetX,targetY,sourcePosition,targetPosition,data,selected,markerEnd,markerStart}=props;const kind=data?.connectorStyle||'smoothstep';let result:[string,number,number,number,number];if(kind==='straight'||kind==='dotted'||kind==='dashed')result=getStraightPath({sourceX,sourceY,targetX,targetY});else if(kind==='bezier')result=getBezierPath({sourceX,sourceY,targetX,targetY,sourcePosition,targetPosition});else result=getSmoothStepPath({sourceX,sourceY,targetX,targetY,sourcePosition,targetPosition,borderRadius:8,offset:24});const[path,labelX,labelY]=result;const dash=kind==='dotted'?'3 7':kind==='dashed'?'10 7':undefined;const label=[data?.label,data?.connectionType,data?.protocol&&data?.port?`${data.protocol} ${data.port}`:data?.protocol||data?.port].filter(Boolean).join(' · ');return <><BaseEdge id={id} path={path} markerEnd={markerEnd} markerStart={markerStart} style={{stroke:selected?'#2563eb':'#64748b',strokeWidth:selected?3:1.8,strokeDasharray:dash}}/>{label&&<EdgeLabelRenderer><div className="edge-label" style={{transform:`translate(-50%, -50%) translate(${labelX}px,${labelY}px)`}}>{label}</div></EdgeLabelRenderer>}</>}
